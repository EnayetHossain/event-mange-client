import "./Profile.css";

const Profile = () => {
    return (
        <section className="desktop-max">
            <span className="work-sans font-bold text-3xl">Profile Information</span>
        </section>
    );
};

export default Profile;